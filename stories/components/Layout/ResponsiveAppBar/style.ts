import styled from "@emotion/styled";
import { AppBar } from "@mui/material";

export const FioAppBar = styled(AppBar)({
    backgroundColor: 'unset',
    // background: 'transparent',
    color: 'black',
    boxShadow: 'none',
});