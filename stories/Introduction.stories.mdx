// import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';
import { Meta, Title, IconGallery, IconItem,Canvas, Story } from '@storybook/addon-docs/';

import { Text } from './Text.tsx';
import { FormInput } from './components/FormInputElement/index.tsx';
import { CleanFilesIcon } from './Icons/CleanFilesIcon.tsx';
import { IconGallery } from 'storybook-icon-gallery';
import Icons from './Icons';

<Canvas>
    <Story
        name="Medium"
        args={{
            size: 'Medium',
            animation: false,
        }}
    >
        <h1>Glasswall Icon Library</h1>
            <IconGallery>
                {Object.entries(Icons).map((res) => {
                    console.log('AllComps on its own',res)
                    const name = res.at(0);
                    const Icon = res.at(1);
                    return (
                        <IconGallery.Variants name={name}>
                            <IconGallery.Variant>
                                <Icon
                                    sx={{ width: 100, height: 100 }}
                                />
                            </IconGallery.Variant>
                        </IconGallery.Variants>
                    );
                })}
            </IconGallery>
            
    </Story>
</Canvas>
